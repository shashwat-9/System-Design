## Liskov Substitution Principle

Making changes around configuration file or
When a class is changing behavior based on the state it has, then rather than doing inheritance, we should do
composition.

The idea is, when we extend multiple classes. 